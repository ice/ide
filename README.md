## Ice auto-completion in IDE

```sh
git clone https://github.com/ice/ide.git
```

### NetBeans
add `ide/1.0.0` to your project's include path or to the global path

### Eclipse
add `ide/1.0.0` to eclipse path